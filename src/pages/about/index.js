import React from 'react'

const AboutPage = () => {
  return (
    <div>
        <h1>About</h1>
        <p>An application that displays hard-coded stock data.
        The application uses React Router to navigate between pages.
        One page displays a table of stock data, and another page displays
        a single stock's full data.
        </p>
    </div>
  )
}

export default AboutPage